<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".Main"
    android:id="@+id/Main">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:gravity="center"
        android:text="Here you can manager your CWM, TWRP, or other Custom Image"
        android:textSize="18sp"
        android:id="@+id/text_top" />

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignLeft="@+id/textView1"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingTop="15dp"
        android:layout_below="@+id/text_top">

        <Button
            android:id="@+id/btn_rec_installer_main"
            android:layout_width="244dp"
            android:layout_height="wrap_content"
            android:onClick="install"
            android:text="Install Recovery" />

        <Button
            android:id="@+id/btn_rec_installer_restore"
            android:layout_width="244dp"
            android:layout_height="wrap_content"
            android:onClick="recoveryInstaller"
            android:text="Restore Stock Recovery" />

        <Button
            android:id="@+id/btn_rec_installer_custom"
            android:layout_width="244dp"
            android:layout_height="wrap_content"
            android:onClick="recoveryInstaller"
            android:text="Install a Custom Image" />

        <Button
            android:id="@+id/btn_rec_installer_reboot"
            android:layout_width="244dp"
            android:layout_height="wrap_content"
            android:onClick="reboot"
            android:text="@string/rebooter" />

    </LinearLayout>


</RelativeLayout>
